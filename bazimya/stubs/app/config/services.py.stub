"""Credentials for third-party services.

A single place for API keys, so they are not scattered through the code.
"""

from bazimya import env

config = {
    # "stripe": {
    #     "key": env("STRIPE_KEY", ""),
    #     "secret": env("STRIPE_SECRET", ""),
    # },
}
